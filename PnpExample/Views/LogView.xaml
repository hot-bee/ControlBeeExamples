<UserControl x:Class="PnpExample.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="100"></Setter>
                    <Setter Property="Margin" Value="1"></Setter>
                    <Setter Property="Background" Value="WhiteSmoke"></Setter>
                </Style>
            </StackPanel.Resources>
            <Button Content="ERROR" Command="{Binding LoadTableCommand}" CommandParameter=""/>
            <Button Content="PARAMETERS" Command="{Binding LoadVariableChangesCommand}" />
            <Button Content="WORK DATA" Command="{Binding LoadTableCommand}" CommandParameter="events"/>

            <TextBlock Text="Start:" VerticalAlignment="Center" Margin="10 0 5 0"/>
            <DatePicker x:Name="StartDatePicker" VerticalAlignment="Center" HorizontalAlignment="Center" SelectedDate="{Binding StartDate, Mode=TwoWay}" Margin="0 0 10 0"/>
            <TextBlock Text="End:" VerticalAlignment="Center" Margin="0 0 5 0"/>
            <DatePicker x:Name="EndDatePicker" VerticalAlignment="Center" HorizontalAlignment="Center" SelectedDate="{Binding EndDate, Mode=TwoWay}" Margin="0 0 10 0"/>
            <Button Content="필터" Height="30" Command="{Binding DateFilterTableCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="5" x:Name="DataGridView" ItemsSource="{Binding CurrentView}" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</UserControl>