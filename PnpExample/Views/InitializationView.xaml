<UserControl x:Class="PnpExample.Views.InitializationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:PnpExample.ViewModels"
             xmlns:components="clr-namespace:ControlBeeWPF.Components;assembly=ControlBeeWPF"
             d:DataContext="{d:DesignInstance Type=viewModels:InitializationViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="Silver" BorderThickness="1">
        <Grid>
            <Grid.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource ExtrudedButtonStyle}" />
                <Style TargetType="components:ToggleImageButton" BasedOn="{StaticResource ExtrudedButtonStyle}" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderBrush="Silver" BorderThickness="0 0 0 1"
                    Background="Khaki">
                <Label Content="Unit Origin" 
                       FontWeight="Bold" Foreground="DarkRed" HorizontalAlignment="Center"/>
            </Border>
            <Border Grid.Row="1" BorderBrush="Silver" BorderThickness="0 0 0 1">
                <WrapPanel Name="WrapPanel1" Margin="10"/>
            </Border>
            <Border Grid.Row="2" BorderBrush="Silver" BorderThickness="0 0 0 0">
                <Grid DockPanel.Dock="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Name="SelectAllButton" Content="All Select" Click="SelectAllButton_OnClick"
                                    Foreground="DarkBlue" FontWeight="Bold" MaxWidth="150" MaxHeight="60" />
                            <Button Grid.Column="1" Name="DeselectAllButton" Content="All Cancel" Click="DeselectAllButton_OnClick"
                                    Foreground="DarkBlue" FontWeight="Bold" MaxWidth="150" MaxHeight="60" />
                        </Grid>
                    </Grid>
                    <Button Grid.Column="2" Name="InitializeAllButton" Content="Unit Origin" 
                        Command="{Binding InitializeCommand}"
                        Foreground="DarkRed" FontWeight="Bold" MaxWidth="150" MaxHeight="60" />
                </Grid>
            </Border>

        </Grid>
    </Border>
</UserControl>