<UserControl x:Class="PnpExample.Views.ManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource ExtrudedButtonWordWrapStyle}">
                <Setter Property="Width" Value="100"></Setter>
                <Setter Property="Height" Value="50"></Setter>
                <Setter Property="Margin" Value="2"></Setter>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="600" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Name="InspectionGroup" Header="Camera" />
        <GroupBox Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <GroupBox Grid.Row="0" Header="Functions">
                    <StackPanel Orientation="Vertical" Margin="10">
                        <StackPanel Name="FunctionVariablePanel" Orientation="Vertical"
                                    HorizontalAlignment="Left"/>
                        <WrapPanel Name="FunctionPanel" />
                    </StackPanel>
                </GroupBox>
                <GroupBox Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <GroupBox Name="JogGroup" Header="Jog" >
                        </GroupBox>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>