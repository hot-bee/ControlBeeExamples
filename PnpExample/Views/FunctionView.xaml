<UserControl x:Class="PnpExample.Views.FunctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Units">
            <WrapPanel Name="UnitsPanel" Orientation="Horizontal">
                <WrapPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="Width" Value="110" />
                        <Setter Property="Height" Value="40" />
                        <Setter Property="Margin" Value="1" />
                        <Setter Property="Background" Value="WhiteSmoke" />
                    </Style>
                </WrapPanel.Resources>
                <Button Content="Unit1"></Button>
                <Button Content="Unit1"></Button>
                <Button Content="Unit1"></Button>
            </WrapPanel>
        </GroupBox>
        <ContentControl Grid.Row="1" Name="ContentArea"></ContentControl>
    </Grid>
</UserControl>