<UserControl x:Class="PnpExample.Views.TeachingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels;assembly=ControlBeeWPF"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:TeachingViewModel}">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource ExtrudedButtonStyle}"></Style>
            <Style TargetType="TabItem">
                <Setter Property="Padding" Value="10 5" />
                <Setter Property="MinWidth" Value="80" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
            </Style>
            <Style TargetType="GroupBox">
                <Setter Property="Margin" Value="5" />
                <Setter Property="Padding" Value="5" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Header="Positions" >
            <ScrollViewer VerticalScrollBarVisibility="Auto" >
                <ListBox Name="PositionItemList"
                         SelectionChanged="PositionItemList_OnSelectionChanged" />
            </ScrollViewer>
        </GroupBox>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="150" />
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Name="CameraGroup" Header="Camera"></GroupBox>
            <TabControl Grid.Row="1">
                <TabItem Header="Data">
                    <ContentControl Name="DataContent"/>
                </TabItem>
                <TabItem Header="AxisStatus">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ContentControl Name="AxisStatusContent"/>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
        <GroupBox Grid.Column="2" Header="Controls">
            <TabControl>
                <TabItem Header="Jog">
                    <ContentControl Name="JogContent" />
                </TabItem>
                <TabItem Header="Functions">
                    <WrapPanel Name="FunctionPanel" Orientation="Horizontal" />
                </TabItem>
                <TabItem Header="IO">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Row="0" Grid.Column="0" Header="Inputs" >
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Name="InputsPanel" />
                            </ScrollViewer>
                        </GroupBox>
                        <TabControl Grid.Row="1" >
                            <TabItem Header="Outputs">
                                <GroupBox Header="Outputs" >
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel Name="OutputsPanel" />
                                    </ScrollViewer>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="Binary Actuators">
                                <GroupBox Header="Binary Actuators" >
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel Name="BinaryActuatorsPanel" />
                                    </ScrollViewer>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </TabItem>
            </TabControl>
        </GroupBox>
    </Grid>
</UserControl>