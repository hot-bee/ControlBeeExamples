<UserControl x:Class="PnpExample.Views.MonitorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:PnpExample.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:AutoOperationViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="...">
                <StackPanel>
                    <Button Content="..." Height="60" Margin="5"/>
                </StackPanel>
            </GroupBox>
        </Grid>
        <GroupBox Grid.Column="1" Header="...">
            <StackPanel>
                <Button Content="..." Height="60" Margin="5"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Column="2" Header="...">
            <StackPanel>
                <Button Content="..." Height="60" Margin="5"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Column="3" Header="Servo Status">
            <StackPanel>
                <Button Name="ServoStatusButton" Content="Servo Status" 
                        Click="Btn_OnClick"
                        Height="60" Margin="5"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>