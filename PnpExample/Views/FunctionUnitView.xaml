<UserControl x:Class="PnpExample.Views.FunctionUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <ContentControl Grid.Row="0" Name="MainViewArea" />
                <ContentControl Grid.Row="1" Name="SubViewArea" />
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="단일 동작 펑션">
                <ContentControl>
                    <WrapPanel Name="FunctionPanel" Margin="10">
                        <Button Content="Function1" Height="40" Width="100" Margin="5" />
                        <Button Content="Function2" Height="40" Width="100" Margin="5" />
                        <Button Content="Function3" Height="40" Width="100" Margin="5" />
                    </WrapPanel>
                </ContentControl>
            </GroupBox>
            <Grid Grid.Row="1">
                <ContentControl Name="CustomArea" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>